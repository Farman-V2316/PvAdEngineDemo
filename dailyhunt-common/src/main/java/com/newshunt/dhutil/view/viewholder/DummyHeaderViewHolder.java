/*
 * Copyright (c) 2015 Newshunt. All rights reserved.
 */

package com.newshunt.dhutil.view.viewholder;

import androidx.recyclerview.widget.RecyclerView;
import android.view.View;
import android.widget.ImageView;
import android.widget.LinearLayout;
import android.widget.TextView;

import com.newshunt.dhutil.R;


/**
 * Dummy place holder for slide up effect.
 *
 * @author nilesh.borkar
 *//*

public class DummyHeaderViewHolder extends RecyclerView.ViewHolder {

  private LinearLayout parent;
  private ImageView subTopicFavButton;
  private LinearLayout subTopicFavParent;
  private LinearLayout subTopicButton;
  private TextView titleFavText;
  private View view;

  public DummyHeaderViewHolder(View itemView) {
    super(itemView);

    this.view = itemView;
    parent = (LinearLayout) itemView.findViewById(R.id.parent);
    subTopicFavButton = (ImageView) itemView.findViewById(R.id.sub_topic_fav_button);
    subTopicFavParent = (LinearLayout) itemView.findViewById(R.id.sub_topic_fav_parent);
    subTopicButton = (LinearLayout) itemView.findViewById(R.id.browse_further_button);
    titleFavText = (TextView) itemView.findViewById(R.id.title_fav_text);
  }

  public View getView() {
    return view;
  }
}
*/
