<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             android:layout_width="match_parent"
             android:layout_height="wrap_content">

    <TextView
        android:id="@+id/grouped_textView"
        style="@style/NotificationTextStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/notification_margin_top_for_grouped_text"
        android:ellipsize="none"
        android:gravity="end"/>

    <!-- For urdu gradient effect is not needed as setMaxLines(1) will take care of word-wrap -->
</FrameLayout>